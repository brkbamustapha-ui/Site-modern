# Optional — used for SEO metadata (Open Graph, sitemap, robots.txt) and metadataBase.
# If unset or invalid, the app falls back to a safe placeholder URL so the build never fails.
NEXT_PUBLIC_SITE_URL="https://msdeveloppements.example"
